services:
  - type: web
    name: e-commerce-store
    env: node
    buildCommand: npm run render-build
    startCommand: npm run render-start
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    regions:
      - oregon
